@model Bookkeeping.Models.TallyRecord
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_TallyLayout.cshtml";
}

<div class="well" style="margin-top:50px">
    <form class="form-horizontal">
        <div class="form-group">
            <label for="category" class="col-sm-2 control-label">@Html.DisplayNameFor(m=>m.Category)</label>
            <div class="col-sm-10">         
              @Html.EnumDropDownListFor(model=>model.Category, "請選擇", new {@class = "form-control" })
            </div>
        </div>
        <div class="form-group">
            <label for="money" class="col-sm-2 control-label">@Html.DisplayNameFor(m => m.Money)</label>
            <div class="col-sm-10">
                @Html.TextBoxFor(model=>model.Money, new { @class = "form-control", placeholder= Html.DisplayNameFor(m => m.Money) })
            </div>
        </div>
        <div class="form-group">
            <label for="date" class="col-sm-2 control-label">@Html.DisplayNameFor(m => m.Date)</label>
            <div class="col-sm-10">
                @Html.TextBoxFor(model => model.Date, new { @class = "form-control", placeholder = Html.DisplayNameFor(m => m.Date) })
            </div>
        </div>
        <div class="form-group">
            <label for="description" class="col-sm-2 control-label">@Html.DisplayNameFor(m => m.Description)</label>
            <div class="col-sm-10">
                @Html.TextAreaFor(model=>model.Description, new { @class = "form-control"})
            </div>
        </div>
        <div class="form-group">
            <div class="col-sm-offset-8 col-sm-4">
                <button type="submit" class="btn btn-default">送出</button>
            </div>
        </div>
    </form>
</div>
<div id="DetailData">
    @Html.Action("Detail","Tally")
</div>
